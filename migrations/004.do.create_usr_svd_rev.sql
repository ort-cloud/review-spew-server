CREATE TABLE usr_svd_rev (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  users_id INTEGER REFERENCES users(users_id) NOT NULL,
  reviews_id INTEGER REFERENCES reviews(reviews_id) NOT NULL
);